// The trace-analysis core lives in @flightrec/trace-analyze (framework-free, shared with the MCP
// server). Re-exported here so the inspector's imports stay `from "../lib/derive"`.
export * from "@flightrec/trace-analyze";
